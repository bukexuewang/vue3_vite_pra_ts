export const LOGIN = { path: '/login', name: 'login' };

export const HOME = { path: '/', name: 'home' };

export const OTHER = { path: '/other', name: 'other' };

export const DASHBOARD = { path: '/dashboard', name: 'dashBoard' };
